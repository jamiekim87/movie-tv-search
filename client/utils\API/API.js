import axios from 'axios'

const API = {
    getMedia: search => axios.get
    getSavedMedia: () => 
    saveMedia: media axios.delete(`/api/media/${id}`)
    deleteMedia: () => axios.delete(`/api/media/${id}`)
}